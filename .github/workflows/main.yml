name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Install gems
      run: bundle install
    - name: Run the tests
      run: |
        bundle exec rspec
        echo All good!
