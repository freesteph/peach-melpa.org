#themes-list
  %h1 Themes

  #wrapper
    - @themes.each do |theme|
      = link_to(theme_path(theme), { class: "theme-thumbnail" }) do
        %img{src: url_for(theme.screenshot.variant(resize: "600x"))}
        .meta= theme.name
