#themes-list{data: { controller: 'themes' }}
  - @themes.each do |theme|
    .theme
      -# going to prison for the code below
      = link_to([theme, theme.variants.first], { class: "theme-thumbnail" , data: { brightness: theme.brightness }}) do
        - screenshot = theme.variants.first.screenshots.find { |s| s.filename.to_s.include?("_el") }
        %img{src: url_for(screenshot.variant(resize: "600x"))}
        .meta= theme.radical
